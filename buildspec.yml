version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci || npm install
  build:
    commands:
      - npm run build

artifacts:
  base-directory: dist/my-angular-project
  files:
    - '**/*'

